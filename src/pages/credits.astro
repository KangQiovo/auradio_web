---
import Site from '../layouts/Site.astro';
import Arrow from '../components/Arrow.astro';
import {motionSources} from '../lib/content.mjs';
import {route} from '../lib/urls';
---
<Site title="技术与项目鸣谢 — Auradio" light description="本官网使用维护者已 Star 的 Motion、GSAP 与 Three.js，搭配 Astro、React 和 TypeScript。查看真实使用位置与准确许可说明。">
  <div class="wrap"><header class="page-heading"><a class="breadcrumb" href={route()}>Auradio / 技术鸣谢</a><h1>让声音和界面<br/>动起来的工具。</h1><p>从维护者已 Star 的项目中，选了三个各有用途的动效库。<br/>不是只放在依赖列表里，它们分别参与了实际交互。</p></header>
    <section class="credits-list" aria-label="使用的动效库">{motionSources.map((s,i)=><article><span class="credit-number">0{i+1}</span><div><h2>{s.name}</h2><code>{s.repository}</code><p>{s.role}</p><span class="license-label">{s.license}</span></div><a href={`https://github.com/${s.repository}`} target="_blank" rel="noopener noreferrer" aria-label={`查看 ${s.name} 项目`}><Arrow diagonal/></a></article>)}</section>
    <div class="document-content credits-notes"><section><h2>页面轻一点，交互按需来。</h2><p>Astro 预渲染介绍、进度与说明页面。React 只负责需要状态的体验区；Three.js 在声场接近视口时才加载。GSAP 控制章节与阅读进度，不接管滚轮或触摸滚动。</p><p>字体使用系统字体。没有远程图片、音乐平台 SDK、分析 SDK 或自动播放的背景音频。页面不可见、声场离屏或启用减少动态时，持续渲染会停止。</p></section><section><h2>许可与来源</h2><p>Motion 与 Three.js 使用 MIT 许可。GSAP 使用自己的 Standard License，不标为 MIT，也不将其当作 OSI 开源许可。构建时保留所用软件包随附的许可文本；GSAP 保留声明并链接完整条款。它们列于站点的<a href={route('licenses/index.json')}>许可索引</a>。</p><p>上述三个项目的 Star 来源核对日期为 2026-09-21；依据公开 Star 审计记录。官网代码公开不改变 Auradio 客户端源码的私有状态。</p><p>Auradio 的 A + 声波图标来自项目已有品牌素材，本官网沿用该资产。网页中的声场几何与合成音频由本项目代码生成，不使用第三方歌曲、专辑封面或客户端未授权截图。</p></section></div>
  </div>
</Site>
